import React, { FC } from 'react';
import { Container, Decoration, Title } from './Footer.Style';

const Footer: FC = () => {
	return (
		<Container>
			<Title>Footer</Title>
			<Decoration />
		</Container>
	);
};

export default Footer;
