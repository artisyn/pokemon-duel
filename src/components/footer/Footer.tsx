import React, { FC } from 'react';
import { Container, Decoration } from './Footer.Style';

const Footer: FC = () => {
	return (
		<Container>
			<Decoration />
		</Container>
	);
};

export default Footer;
